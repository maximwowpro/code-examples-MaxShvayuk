In this program you can set a table with X and Y values with const step and later count a Y = func(X0), (X0 isn`t in our table).
X0 isn`t in a table.
For example I use atan function and compare counted value with true value from the library.

My program use 1 interpolation Newton`s formula.
