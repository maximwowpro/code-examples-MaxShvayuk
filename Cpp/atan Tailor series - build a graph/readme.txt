A program which count Y = atan(X) using Tailor series and comparise counted value with true value, taken from the library. 
Also it builds a graph (green - libraru values, blue - values counted by my program).
